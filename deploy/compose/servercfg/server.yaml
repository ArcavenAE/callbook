log_level: info

behavior:
  read_only: false
  autocommit: true

listener:
  host: 0.0.0.0
  port: 3306
  max_connections: 100
  # TLS for anything beyond localhost (callbook rule). Mount certs and uncomment:
  # tls_key: /etc/dolt/tls/tls.key
  # tls_cert: /etc/dolt/tls/tls.crt
  # require_secure_transport: true

remotesapi:
  port: 8000
